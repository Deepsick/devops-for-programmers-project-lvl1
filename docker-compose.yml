version: "3.3"

services:
  db:
    image: postgres
    restart: always
    env_file: 
      - .env
    environment: 
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
  app:
    build: .
    depends_on: 
      - db
